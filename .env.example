# Server-only Gemini key. Create it in Google AI Studio; never expose it with a NEXT_PUBLIC_ prefix.
GEMINI_API_KEY=

# Public origin used for canonical and benchmark share URLs.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional bearer token for the independent BenchmarkList refresh clock.
BENCHMARK_SYNC_TOKEN=
